# Can you change the values inside a list which is contained in set S?
# S =  {8, 7, 12, "Harry", [1,2]}

'''
Ans - 
No, you cannot include a list as an element in a set in Python. 
This is because lists are mutable, and sets only allow hashable (immutable) objects as their elements
'''